/**
 * Cost — placeholder stub. Task 1 wires the nav item and route; the real
 * Cost screen (StatTiles, CostChips, engine-backed spend data) lands in
 * Task 3. This stub exists so /cost resolves and the e2e nav walk has
 * something to assert against in the meantime.
 */
export function CostPage() {
  return <main aria-busy="true" className="p-8 text-slate-400">Cost</main>;
}
